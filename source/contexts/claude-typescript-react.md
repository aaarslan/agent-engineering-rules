---
scope: [context]
load_when: matching TypeScript or React paths on Claude Code
related: [typescript-react.md, web-ui.md]
---

# TypeScript and React Route

Before changing a matching TypeScript or React file, read `agent-rules/reference/typescript-react.md`; for browser UI, also read `agent-rules/reference/web-ui.md`.
