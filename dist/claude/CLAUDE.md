# AER index

Contract/profile: `.claude/rules/`. Skills: `.claude/skills/`; `/autonomous-mission` is explicit-only.

Review packets: paths and complete scoped diff for `/pr-review`; trust boundaries and entrypoints for `/aer-security-review`.

Consult full references only when they resolve uncertainty beyond the kernel/repository contract: `agent-rules/reference/testing.md`, `agent-rules/reference/security.md`.
